# HG changeset patch
# Parent 46c656ec089310253d14ce6ad2f03a7498b2584c
diff --git a/src/os_win32.c b/src/os_win32.c
--- a/src/os_win32.c
+++ b/src/os_win32.c
@@ -5996,14 +5996,14 @@ mch_rename(
  * Returns 0 for success, -1 for failure.
  */
     int
-mch_rmdir(const char *dirname)
+mch_rmdir(char_u *dirname)
 {
     BOOL result = FALSE;
 
 #ifdef FEAT_MBYTE
     if (enc_codepage >= 0 && (int)GetACP() != enc_codepage)
     {
-	WCHAR	*wdirname = enc_to_utf16((char_u *)dirname, NULL);
+	WCHAR	*wdirname = enc_to_utf16(dirname, NULL);
 	if (wdirname != NULL)
 	{
 	    result = RemoveDirectoryW(wdirname);
@@ -6014,7 +6014,7 @@ mch_rmdir(const char *dirname)
 #endif
     {
 	if (dirname != NULL)
-	    result = RemoveDirectoryA(dirname);
+	    result = RemoveDirectoryA((LPSTR)dirname);
     }
 
     return result != FALSE ? 0 : -1;
diff --git a/src/proto/os_win32.pro b/src/proto/os_win32.pro
--- a/src/proto/os_win32.pro
+++ b/src/proto/os_win32.pro
@@ -45,7 +45,7 @@ int mch_remove __ARGS((char_u *name));
 void mch_breakcheck __ARGS((void));
 int mch_wrename __ARGS((WCHAR *wold, WCHAR *wnew));
 int mch_rename __ARGS((const char *pszOldFile, const char *pszNewFile));
-int mch_rmdir __ARGS((const char *dirname));
+int mch_rmdir __ARGS((char_u *dirname));
 char *default_shell __ARGS((void));
 int mch_access __ARGS((char *n, int p));
 int mch_open __ARGS((char *name, int flags, int mode));
