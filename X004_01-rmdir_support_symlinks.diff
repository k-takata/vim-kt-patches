# HG changeset patch
# Parent 4dac7e21a69929191766158902837ef22166fbdc

diff -r 4dac7e21a699 src/os_mswin.c
--- a/src/os_mswin.c	Sun Sep 01 13:44:28 2013 +0900
+++ b/src/os_mswin.c	Sun Sep 01 13:47:36 2013 +0900
@@ -514,6 +514,12 @@
     char	buf[_MAX_PATH + 1];
 #endif
     char	*p;
+#if defined(_MSC_VER) && _MSC_VER < 1700
+    WIN32_FIND_DATA	findData;
+    HANDLE		hFind, h;
+    DWORD		attr;
+    BOOL		is_symlink = FALSE;
+#endif
 
     vim_strncpy((char_u *)buf, (char_u *)name, sizeof(buf) - 1);
     p = buf + strlen(buf);
@@ -534,6 +540,38 @@
 
 	if (wp != NULL)
 	{
+# if defined(_MSC_VER) && _MSC_VER < 1700
+	    /* Work around for VC10 or earlier. _wstat() can't handle a
+	     * symlink to a directory. */
+	    WIN32_FIND_DATAW	findDataW;
+
+	    hFind = FindFirstFileW(wp, &findDataW);
+	    if (hFind != INVALID_HANDLE_VALUE)
+	    {
+		attr = findDataW.dwFileAttributes;
+		if ((attr & FILE_ATTRIBUTE_REPARSE_POINT)
+			&& (attr & FILE_ATTRIBUTE_DIRECTORY)
+			&& (findDataW.dwReserved0 == IO_REPARSE_TAG_SYMLINK))
+		    is_symlink = TRUE;
+		FindClose(hFind);
+	    }
+	    if (is_symlink)
+	    {
+		h = CreateFileW(wp, FILE_READ_ATTRIBUTES,
+			FILE_SHARE_READ | FILE_SHARE_WRITE, NULL,
+			OPEN_EXISTING, FILE_FLAG_BACKUP_SEMANTICS, NULL);
+		if (h != INVALID_HANDLE_VALUE)
+		{
+		    int	    fd;
+
+		    fd = _open_osfhandle((intptr_t)h, _O_RDONLY);
+		    n = _fstat(fd, (struct _stat*)stp);
+		    _close(fd);
+		    vim_free(wp);
+		    return n;
+		}
+	    }
+# endif
 	    n = _wstat(wp, (struct _stat *)stp);
 	    vim_free(wp);
 	    if (n >= 0)
@@ -544,6 +582,35 @@
 	}
     }
 #endif
+#if defined(_MSC_VER) && _MSC_VER < 1700
+    /* Work around for VC10 or earlier. _stat() can't handle a
+     * symlink to a directory. */
+    hFind = FindFirstFile(buf, &findData);
+    if (hFind != INVALID_HANDLE_VALUE)
+    {
+	attr = findData.dwFileAttributes;
+	if ((attr & FILE_ATTRIBUTE_REPARSE_POINT)
+		&& (attr & FILE_ATTRIBUTE_DIRECTORY)
+		&& (findData.dwReserved0 == IO_REPARSE_TAG_SYMLINK))
+	    is_symlink = TRUE;
+	FindClose(hFind);
+    }
+    if (is_symlink)
+    {
+	h = CreateFile(buf, FILE_READ_ATTRIBUTES,
+		FILE_SHARE_READ | FILE_SHARE_WRITE, NULL,
+		OPEN_EXISTING, FILE_FLAG_BACKUP_SEMANTICS, NULL);
+	if (h != INVALID_HANDLE_VALUE)
+	{
+	    int	    fd, n;
+
+	    fd = _open_osfhandle((intptr_t)h, _O_RDONLY);
+	    n = _fstat(fd, (struct _stat*)stp);
+	    _close(fd);
+	    return n;
+	}
+    }
+#endif
     return stat(buf, stp);
 }
 
diff -r 4dac7e21a699 src/os_unix.c
--- a/src/os_unix.c	Sun Sep 01 13:44:28 2013 +0900
+++ b/src/os_unix.c	Sun Sep 01 13:47:36 2013 +0900
@@ -2943,6 +2943,28 @@
 #endif
 }
 
+/*
+ * return TRUE if "name" is a symbolic link
+ * return FALSE if "name" is not a symbolic link
+ * return FALSE for error
+ */
+    int
+mch_is_symbolic_link(name)
+    char_u *name;
+{
+    struct stat statb;
+
+    if (*name == NUL)	    /* Some lstat()s don't flag "" as an error. */
+	return FALSE;
+    if (lstat((char *)name, &statb))
+	return FALSE;
+#ifdef _POSIX_SOURCE
+    return (S_ISLNK(statb.st_mode) ? TRUE : FALSE);
+#else
+    return ((statb.st_mode & S_IFMT) == S_IFLNK ? TRUE : FALSE);
+#endif
+}
+
 static int executable_file __ARGS((char_u *name));
 
 /*
diff -r 4dac7e21a699 src/os_unix.h
--- a/src/os_unix.h	Sun Sep 01 13:44:28 2013 +0900
+++ b/src/os_unix.h	Sun Sep 01 13:47:36 2013 +0900
@@ -568,3 +568,5 @@
 
 /* We have three kinds of ACL support. */
 #define HAVE_ACL (HAVE_POSIX_ACL || HAVE_SOLARIS_ACL || HAVE_AIX_ACL)
+
+#define FEAT_SYMBOLIC_LINKS
diff -r 4dac7e21a699 src/os_win32.c
--- a/src/os_win32.c	Sun Sep 01 13:44:28 2013 +0900
+++ b/src/os_win32.c	Sun Sep 01 13:47:36 2013 +0900
@@ -78,16 +78,6 @@
 # endif
 #endif
 
-/*
- * Reparse Point
- */
-#ifndef FILE_ATTRIBUTE_REPARSE_POINT
-# define FILE_ATTRIBUTE_REPARSE_POINT	0x00000400
-#endif
-#ifndef IO_REPARSE_TAG_SYMLINK
-# define IO_REPARSE_TAG_SYMLINK		0xA000000C
-#endif
-
 /* Record all output and all keyboard & mouse input */
 /* #define MCH_WRITE_DUMP */
 
@@ -2967,10 +2957,10 @@
 }
 
 /*
- * Return TRUE if file "fname" is a symbolic link.
+ * Return TRUE if "name" is a symbolic link (or a junction).
  */
     int
-mch_is_symbolic_link(char_u *fname)
+mch_is_symbolic_link(char_u *name)
 {
     HANDLE		hFind;
     int			res = FALSE;
@@ -2981,7 +2971,7 @@
     WIN32_FIND_DATAW	findDataW;
 
     if (enc_codepage >= 0 && (int)GetACP() != enc_codepage)
-	wn = enc_to_utf16(fname, NULL);
+	wn = enc_to_utf16(name, NULL);
     if (wn != NULL)
     {
 	hFind = FindFirstFileW(wn, &findDataW);
@@ -2990,7 +2980,7 @@
 		&& GetLastError() == ERROR_CALL_NOT_IMPLEMENTED)
 	{
 	    /* Retry with non-wide function (for Windows 98). */
-	    hFind = FindFirstFile(fname, &findDataA);
+	    hFind = FindFirstFile(name, &findDataA);
 	    if (hFind != INVALID_HANDLE_VALUE)
 	    {
 		fileFlags = findDataA.dwFileAttributes;
@@ -3006,7 +2996,7 @@
     else
 #endif
     {
-	hFind = FindFirstFile(fname, &findDataA);
+	hFind = FindFirstFile(name, &findDataA);
 	if (hFind != INVALID_HANDLE_VALUE)
 	{
 	    fileFlags = findDataA.dwFileAttributes;
@@ -3018,7 +3008,8 @@
 	FindClose(hFind);
 
     if ((fileFlags & FILE_ATTRIBUTE_REPARSE_POINT)
-	    && reparseTag == IO_REPARSE_TAG_SYMLINK)
+	    && (reparseTag == IO_REPARSE_TAG_SYMLINK
+		|| reparseTag == IO_REPARSE_TAG_MOUNT_POINT))
 	res = TRUE;
 
     return res;
@@ -5500,7 +5491,8 @@
 
 
 /*
- * this version of remove is not scared by a readonly (backup) file
+ * This version of remove is not scared by a readonly (backup) file.
+ * This can also remove a symbolic link like Unix.
  * Return 0 for success, -1 for failure.
  */
     int
@@ -5511,6 +5503,9 @@
     int		n;
 #endif
 
+    if (mch_isdir(name) && mch_is_symbolic_link(name))
+	return mch_rmdir(name);
+
     win32_setattrs(name, FILE_ATTRIBUTE_NORMAL);
 
 #ifdef FEAT_MBYTE
diff -r 4dac7e21a699 src/os_win32.h
--- a/src/os_win32.h	Sun Sep 01 13:44:28 2013 +0900
+++ b/src/os_win32.h	Sun Sep 01 13:47:36 2013 +0900
@@ -57,6 +57,8 @@
 
 #define FEAT_SHORTCUT		/* resolve shortcuts */
 
+#define FEAT_SYMBOLIC_LINKS
+
 #if (!defined(__BORLANDC__) || __BORLANDC__ >= 0x550) \
 	&& (!defined(_MSC_VER) || _MSC_VER > 1020)
 /*
@@ -130,6 +132,19 @@
 # define DFLT_MAXMEMTOT	(5*1024)    /* use up to 5 Mbyte for Vim */
 #endif
 
+/*
+ * Reparse Point
+ */
+#ifndef FILE_ATTRIBUTE_REPARSE_POINT
+# define FILE_ATTRIBUTE_REPARSE_POINT	0x00000400
+#endif
+#ifndef IO_REPARSE_TAG_MOUNT_POINT
+# define IO_REPARSE_TAG_MOUNT_POINT	0xA0000003
+#endif
+#ifndef IO_REPARSE_TAG_SYMLINK
+# define IO_REPARSE_TAG_SYMLINK		0xA000000C
+#endif
+
 #if defined(_MSC_VER) || defined(__BORLANDC__)
     /* Support for __try / __except.  All versions of MSVC and Borland C are
      * expected to have this.  Any other compilers that support it? */
diff -r 4dac7e21a699 src/proto/os_unix.pro
--- a/src/proto/os_unix.pro	Sun Sep 01 13:44:28 2013 +0900
+++ b/src/proto/os_unix.pro	Sun Sep 01 13:47:36 2013 +0900
@@ -42,6 +42,7 @@
 void mch_free_acl __ARGS((vim_acl_T aclent));
 void mch_hide __ARGS((char_u *name));
 int mch_isdir __ARGS((char_u *name));
+int mch_is_symbolic_link __ARGS((char_u *name));
 int mch_can_exe __ARGS((char_u *name));
 int mch_nodetype __ARGS((char_u *name));
 void mch_early_init __ARGS((void));
