# HG changeset patch
# Parent  57078b938495d3d6d97ca4e6c452a91f7a31a01f

diff --git a/src/eval.c b/src/eval.c
--- a/src/eval.c
+++ b/src/eval.c
@@ -6829,6 +6829,7 @@ check_vars(
     {
 	if (find_var(name, NULL, TRUE) != NULL)
 	{
+	    TRACE("lvar: %s", varname);
 	    *eval_lavars_used = TRUE;
 	}
     }
@@ -6836,6 +6837,7 @@ check_vars(
     {
 	if (find_var(name, NULL, TRUE) != NULL)
 	{
+	    TRACE("avar: %s", varname);
 	    *eval_lavars_used = TRUE;
 	}
     }
diff --git a/src/userfunc.c b/src/userfunc.c
--- a/src/userfunc.c
+++ b/src/userfunc.c
@@ -334,6 +334,7 @@ get_lambda_tv(char_u **arg, typval_T *re
 	((char_u **)(newlines.ga_data))[newlines.ga_len++] = p;
 	STRCPY(p, "return ");
 	vim_strncpy(p + 7, s, e - s);
+	TRACE("lambda: la:%d, name: %s, expr: %s", eval_lavars, name, p + 7);
 
 	fp->uf_refcount = 1;
 	STRCPY(fp->uf_name, name);
@@ -345,6 +346,7 @@ get_lambda_tv(char_u **arg, typval_T *re
 	    fp->uf_scoped = current_funccal;
 	    current_funccal->fc_refcount++;
 	    func_ref(current_funccal->func->uf_name);
+	    TRACE("name:%s, fc_refcount:%d", current_funccal->func->uf_name, current_funccal->fc_refcount);
 	}
 	else
 	    fp->uf_scoped = NULL;
@@ -661,6 +663,7 @@ free_funccal(
 	for (li = fc->l_varlist.lv_first; li != NULL; li = li->li_next)
 	    clear_tv(&li->li_tv);
 
+    TRACE("free_funccal: %s", fc->func->uf_name);
     vim_free(fc);
 }
 
@@ -1089,6 +1092,8 @@ func_free(ufunc_T *fp)
 	func_unref(fp->uf_scoped->func->uf_name);
     }
 
+    TRACE("func_free: %s", fp->uf_name);
+
     vim_free(fp);
 }
 
@@ -2684,6 +2689,7 @@ func_unref(char_u *name)
 	    if (fp->uf_calls == 0)
 		func_free(fp);
 	}
+	TRACE("func_unref'ed: %s, cnt:%d", name, fp ? fp->uf_refcount : -1);
     }
     else if (STRNCMP(name, "<lambda>", 8) == 0)
     {
@@ -2696,6 +2702,7 @@ func_unref(char_u *name)
 	    if (fp->uf_calls == 0)
 		func_free(fp);
 	}
+	TRACE("func_unref'ed: %s, cnt:%d", name, fp ? fp->uf_refcount : -1);
     }
 }
 
@@ -2716,6 +2723,7 @@ func_ref(char_u *name)
 	    EMSG2(_(e_intern2), "func_ref()");
 	else
 	    ++fp->uf_refcount;
+	TRACE("func_ref: %s, cnt:%d", name, fp ? fp->uf_refcount : -1);
     }
     else if (STRNCMP(name, "<lambda>", 8) == 0)
     {
@@ -2723,6 +2731,7 @@ func_ref(char_u *name)
 	fp = find_func(name);
 	if (fp != NULL)
 	    ++fp->uf_refcount;
+	TRACE("func_ref: %s, cnt:%d", name, fp ? fp->uf_refcount : -1);
     }
 }
 
