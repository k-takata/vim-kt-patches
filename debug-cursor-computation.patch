# HG changeset patch
# Parent  cd7e6d71268cd00c0230b82fe4b8c70e122d00cc

diff --git a/src/charset.c b/src/charset.c
--- a/src/charset.c
+++ b/src/charset.c
@@ -852,10 +852,26 @@ linetabsize_no_outer(win_T *wp, linenr_T
     void
 win_linetabsize_cts(chartabsize_T *cts, colnr_T len)
 {
+    static char_u   *old_line = NULL;
+    static char_u   *saved_ptr = NULL;
+    static colnr_T  saved_vcol = 0;
+    colnr_T slen = len;
     vimlong_T vcol = cts->cts_vcol;
+
+    proftime_T tm;
+    profile_start(&tm);
+    Trace("win_linetabsize_cts: cts->cts_line=%p cts->cts_ptr=%p cts->cts_vcol=%d\n", cts->cts_line, cts->cts_ptr, cts->cts_vcol);
+
 #ifdef FEAT_PROP_POPUP
     cts->cts_with_trailing = len == MAXCOL;
 #endif
+    if (slen == MAXCOL)
+	slen = STRLEN(cts->cts_line);
+    if (old_line == cts->cts_line)
+    {
+	cts->cts_ptr = saved_ptr;
+	vcol = saved_vcol;
+    }
     for ( ; *cts->cts_ptr != NUL && (len == MAXCOL || cts->cts_ptr < cts->cts_line + len);
 						      MB_PTR_ADV(cts->cts_ptr))
     {
@@ -867,6 +883,14 @@ win_linetabsize_cts(chartabsize_T *cts, 
 	}
 	else
 	    cts->cts_vcol = (int)vcol;
+
+	if ((cts->cts_ptr - cts->cts_line <= slen * 4 / 5)
+		&& (cts->cts_ptr + (*mb_ptr2len)(cts->cts_ptr) - cts->cts_line
+			>= slen * 4 / 5))
+	{
+	    saved_ptr = cts->cts_ptr;
+	    saved_vcol = vcol;
+	}
     }
 #ifdef FEAT_PROP_POPUP
     // check for a virtual text at the end of a line or on an empty line
@@ -881,6 +905,9 @@ win_linetabsize_cts(chartabsize_T *cts, 
 	cts->cts_vcol = vcol > MAXCOL ? MAXCOL : (int)vcol;
     }
 #endif
+    old_line = cts->cts_line;
+    profile_end(&tm);
+    Trace("win_linetabsize_cts end, %s", profile_msg(&tm));
 }
 
 /*
@@ -1190,6 +1217,9 @@ win_lbr_chartabsize(
     int		no_sbr = FALSE;
 #endif
 
+    proftime_T tm;
+    //Trace("win_lbr_chartabsize: cts->cts_line=%p cts->cts_ptr=%p cts->cts_vcol=%d\n", cts->cts_line, cts->cts_ptr, cts->cts_vcol);
+    profile_start(&tm);
 #if defined(FEAT_PROP_POPUP)
     cts->cts_cur_text_width = 0;
     cts->cts_first_char = 0;
@@ -1211,7 +1241,12 @@ win_lbr_chartabsize(
 #endif
     {
 	if (wp->w_p_wrap)
-	    return win_nolbr_chartabsize(cts, headp);
+	{
+	    int ret = win_nolbr_chartabsize(cts, headp);
+	    profile_end(&tm);
+	    //Trace("win_lbr_chartabsize 1: %s, cts->cts_vcol=%d\n", profile_msg(&tm), cts->cts_vcol);
+	    return ret;
+	}
 	RET_WIN_BUF_CHARTABSIZE(wp, wp->w_buffer, s, vcol)
     }
 
@@ -1472,6 +1507,8 @@ win_lbr_chartabsize(
 #  ifdef FEAT_PROP_POPUP
     size += cts->cts_first_char;
 #  endif
+    profile_end(&tm);
+    //Trace("win_lbr_chartabsize 2: %s, cts->cts_vcol=%d\n", profile_msg(&tm), cts->cts_vcol);
     return size;
 # endif
 #endif
@@ -1569,7 +1606,13 @@ getvcol(
 #ifdef FEAT_PROP_POPUP
     int		on_NUL = FALSE;
 #endif
+    static char_u   *old_line = NULL;
+    static char_u   *saved_ptr = NULL;
+    static colnr_T  saved_vcol = 0;
 
+    proftime_T tm;
+    profile_start(&tm);
+    Trace("getvcol");
     vcol = 0;
     line = ptr = ml_get_buf(wp->w_buffer, pos->lnum, FALSE);
 
@@ -1591,6 +1634,13 @@ getvcol(
 #endif
        )
     {
+	Trace("getvcol 1");
+	if ((old_line == line)
+		&& (saved_ptr - line <= pos->col))
+	{
+	    ptr = saved_ptr;
+	    vcol = saved_vcol;
+	}
 	for (;;)
 	{
 	    head = 0;
@@ -1634,6 +1684,12 @@ getvcol(
 	    }
 
 	    char_u *next_ptr = ptr + (*mb_ptr2len)(ptr);
+	    if ((ptr - line <= pos->col * 4 / 5)
+		    && (next_ptr - line >= pos->col * 4 / 5))
+	    {
+		saved_ptr = ptr;
+		saved_vcol = vcol;
+	    }
 	    if (next_ptr - line > pos->col) // character at pos->col
 		break;
 
@@ -1643,6 +1699,7 @@ getvcol(
     }
     else
     {
+	Trace("getvcol 2");
 	for (;;)
 	{
 	    // A tab gets expanded, depending on the current column.
@@ -1710,6 +1767,9 @@ getvcol(
 	    *cursor = vcol + head;	    // cursor at start
 	}
     }
+    old_line = line;
+    profile_end(&tm);
+    Trace("getvcol end, %s", profile_msg(&tm));
 }
 
 /*
@@ -1746,6 +1806,7 @@ getvvcol(
     colnr_T	endadd;
     char_u	*ptr;
 
+    Trace("getvvcol");
     if (virtual_active())
     {
 	// For virtual mode, only want one value
@@ -1778,6 +1839,7 @@ getvvcol(
     }
     else
 	getvcol(wp, pos, start, cursor, end);
+    Trace("getvvcol end");
 }
 
 /*
diff --git a/src/misc1.c b/src/misc1.c
--- a/src/misc1.c
+++ b/src/misc1.c
@@ -407,6 +407,8 @@ plines_win_nofold(win_T *wp, linenr_T ln
     int		width;
     chartabsize_T cts;
 
+    proftime_T tm;
+    profile_start(&tm);
     s = ml_get_buf(wp->w_buffer, lnum, FALSE);
     init_chartabsize_arg(&cts, wp, lnum, 0, s, s);
     if (*s == NUL
@@ -434,6 +436,8 @@ plines_win_nofold(win_T *wp, linenr_T ln
 	return 1;
     col -= width;
     width += win_col_off2(wp);
+    profile_end(&tm);
+    Trace("plines_win_nofold end, %s", profile_msg(&tm));
     return (col + (width - 1)) / width + 1;
 }
 
diff --git a/src/move.c b/src/move.c
--- a/src/move.c
+++ b/src/move.c
@@ -282,9 +282,13 @@ update_topline(void)
     long	*so_ptr = curwin->w_p_so >= 0 ? &curwin->w_p_so : &p_so;
     int		save_so = *so_ptr;
 
+    Trace("update_topline");
     // Cursor is updated instead when this is TRUE for 'splitkeep'.
     if (skip_update_topline)
+    {
+	Trace("update_topline end1");
 	return;
+    }
 
     // If there is no valid screen and when the window height is zero just use
     // the cursor line.
@@ -294,12 +298,16 @@ update_topline(void)
 	curwin->w_topline = curwin->w_cursor.lnum;
 	curwin->w_botline = curwin->w_topline;
 	curwin->w_scbind_pos = 1;
+	Trace("update_topline end2");
 	return;
     }
 
     check_cursor_moved(curwin);
     if (curwin->w_valid & VALID_TOPLINE)
+    {
+	Trace("update_topline end3");
 	return;
+    }
 
     // When dragging with the mouse, don't scroll that quickly
     if (mouse_dragging > 0)
@@ -524,6 +532,7 @@ update_topline(void)
     }
 
     *so_ptr = save_so;
+    Trace("update_topline end");
 }
 
 /*
@@ -838,10 +847,12 @@ cursor_valid(void)
     void
 validate_cursor(void)
 {
+    Trace("validate_cursor");
     check_cursor_lnum();
     check_cursor_moved(curwin);
     if ((curwin->w_valid & (VALID_WCOL|VALID_WROW)) != (VALID_WCOL|VALID_WROW))
 	curs_columns(TRUE);
+    Trace("validate_cursor end");
 }
 
 #if defined(FEAT_GUI) || defined(PROTO)
@@ -876,6 +887,7 @@ curs_rows(win_T *wp)
     long	fold_count;
 #endif
 
+    Trace("curs_rows: wp->w_cline_row=%d, wp->w_cline_height=%d", wp->w_cline_row, wp->w_cline_height);
     // Check if wp->w_lines[].wl_size is invalid
     all_invalid = (!redrawing()
 			|| wp->w_lines_valid == 0
@@ -982,6 +994,7 @@ curs_rows(win_T *wp)
 
     redraw_for_cursorline(curwin);
     wp->w_valid |= VALID_CROW|VALID_CHEIGHT;
+    Trace("curs_rows end: wp->w_cline_row=%d, wp->w_cline_height=%d", wp->w_cline_row, wp->w_cline_height);
 }
 
 /*
@@ -1143,6 +1156,9 @@ curs_columns(
     long	siso = get_sidescrolloff_value();
     int		did_sub_skipcol = FALSE;
 
+    proftime_T tm;
+    Trace("curs_columns: curwin->w_wcol=%d curwin->w_virtcol=%d", curwin->w_wcol, curwin->w_virtcol);
+    profile_start(&tm);
     /*
      * First make sure that w_topline is valid (after moving the cursor).
      */
@@ -1159,6 +1175,9 @@ curs_columns(
     curwin->w_virtcol_first_char = 0;
 #endif
 
+    profile_end(&tm);
+    Trace("curs_columns 1: before getvvcol(), %s", profile_msg(&tm));
+    profile_start(&tm);
     /*
      * Compute the number of virtual columns.
      */
@@ -1178,6 +1197,9 @@ curs_columns(
     extra = curwin_col_off();
     curwin->w_wcol = curwin->w_virtcol + extra;
     endcol += extra;
+    profile_end(&tm);
+    Trace("curs_columns 2: after curwin_col_off(), %s", profile_msg(&tm));
+    profile_start(&tm);
 
     /*
      * Now compute w_wrow, counting screen lines from w_cline_row.
@@ -1301,6 +1323,9 @@ curs_columns(
     else
 	curwin->w_wrow += diff_check_fill(curwin, curwin->w_cursor.lnum);
 #endif
+    profile_end(&tm);
+    Trace("curs_columns 3, %s", profile_msg(&tm));
+    profile_start(&tm);
 
     prev_skipcol = curwin->w_skipcol;
 
@@ -1322,6 +1347,9 @@ curs_columns(
 	    && width2 > 0
 	    && curwin->w_width != 0)
     {
+	profile_end(&tm);
+	Trace("curs_columns 4, %s", profile_msg(&tm));
+	profile_start(&tm);
 	// Cursor past end of screen.  Happens with a single line that does
 	// not fit on screen.  Find a skipcol to show the text around the
 	// cursor.  Avoid scrolling all the time. compute value of "extra":
@@ -1400,11 +1428,16 @@ curs_columns(
 	    win_ins_lines(curwin, 0, extra, FALSE, FALSE);
 	else if (extra < 0)
 	    win_del_lines(curwin, 0, -extra, FALSE, FALSE, 0);
+	profile_end(&tm);
+	Trace("curs_columns 5, %s", profile_msg(&tm));
+	profile_start(&tm);
     }
     else if (!curwin->w_p_sms)
 	curwin->w_skipcol = 0;
     if (prev_skipcol != curwin->w_skipcol)
 	redraw_later(UPD_SOME_VALID);
+    profile_end(&tm);
+    Trace("curs_columns 6, %s", profile_msg(&tm));
 
 #ifdef FEAT_SYN_HL
     redraw_for_cursorcolumn(curwin);
@@ -1426,6 +1459,7 @@ curs_columns(
     curwin->w_valid_skipcol = curwin->w_skipcol;
 
     curwin->w_valid |= VALID_WCOL|VALID_WROW|VALID_VIRTCOL;
+    Trace("curs_columns end: curwin->w_wcol=%d curwin->w_virtcol=%d", curwin->w_wcol, curwin->w_virtcol);
 }
 
 #if (defined(FEAT_EVAL) || defined(FEAT_PROP_POPUP)) || defined(PROTO)
