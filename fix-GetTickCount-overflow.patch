# HG changeset patch
# Parent 1c8104188fbc7c9fd045a5e693f927dd798ad527
diff --git a/src/os_win32.c b/src/os_win32.c
--- a/src/os_win32.c
+++ b/src/os_win32.c
@@ -1362,7 +1362,7 @@
 	{
 	    /* If the specified wait time has passed, return. */
 	    dwNow = GetTickCount();
-	    if (dwNow >= dwEndTime)
+	    if ((long)(dwNow - dwEndTime) >= 0)
 		break;
 	}
 	if (msec != 0)
