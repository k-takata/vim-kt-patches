# HG changeset patch
# Parent  181648822cc6feb45b13c463958162e2f58df201

diff --git a/nsis/vimrc.ini b/nsis/vimrc.ini
--- a/nsis/vimrc.ini
+++ b/nsis/vimrc.ini
@@ -11,22 +11,22 @@ Text=" Key remapping "
 
 [Field 2]
 Type=radiobutton
-Text=Do not remap keys for Windows behavior
+Text=Do not remap keys for Windows behavior (Default)
 Left=10
 Right=-10
 Top=17
 Bottom=25
-State=0
+State=1
 Flags=GROUP
 
 [Field 3]
 Type=radiobutton
-Text=Remap a few keys for Windows behavior (<C-V>, <C-C>, etc)
+Text=Remap a few keys for Windows behavior (<C-V>, <C-C>, <C-A>, <C-S>, <C-F>, etc)
 Left=10
 Right=-10
 Top=30
-Bottom=38
-State=1
+Bottom=47
+State=0
 Flags=NOTABSTOP
 
 [Field 4]
@@ -39,30 +39,30 @@ Text=" Mouse behavior "
 
 [Field 5]
 Type=radiobutton
-Text=Right button extends selection (unix)
+Text=Right button extends selection, left button starts visual mode (the Unix way)
 Left=10
-Right=-10
-Top=72
-Bottom=80
+Right=-2
+Top=68
+Bottom=85
 State=0
 Flags=GROUP
 
 [Field 6]
 Type=radiobutton
-Text=Right button has a popup menu, Left button starts selection (mswin)
+Text=Right button has a popup menu, left button starts select mode (the Windows way)
 Left=10
-Right=-10
-Top=85
-Bottom=93
-State=1
+Right=-2
+Top=90
+Bottom=107
+State=0
 Flags=NOTABSTOP
 
 [Field 7]
 Type=radiobutton
-Text=Right button has a popup menu, Left button starts visual mode
+Text=Right button has a popup menu, left button starts visual mode (Default)
 Left=10
-Right=-10
-Top=98
-Bottom=106
-State=0
+Right=-2
+Top=112
+Bottom=120
+State=1
 Flags=NOTABSTOP
diff --git a/src/dosinst.c b/src/dosinst.c
--- a/src/dosinst.c
+++ b/src/dosinst.c
@@ -78,9 +78,9 @@ char	*(remap_choices[]) =
 {
     "\nChoose:",
     "Do not remap keys for Windows behavior",
-    "Remap a few keys for Windows behavior (<C-V>, <C-C>, etc)",
+    "Remap a few keys for Windows behavior (<C-V>, <C-C>, <C-A>, <C-S>, <C-F>, etc)",
 };
-int	remap_choice = (int)remap_win;
+int	remap_choice = (int)remap_no;
 char	*remap_text = "- %s";
 
 enum
@@ -93,10 +93,10 @@ char	*(mouse_choices[]) =
 {
     "\nChoose the way how Vim uses the mouse:",
     "right button extends selection (the Unix way)",
-    "right button has a popup menu, left button starts selection (the Windows way)",
+    "right button has a popup menu, left button starts select mode (the Windows way)",
     "right button has a popup menu, left button starts visual mode",
 };
-int	mouse_choice = (int)mouse_mswin;
+int	mouse_choice = (int)mouse_default;
 char	*mouse_text = "- The mouse %s";
 
 enum
