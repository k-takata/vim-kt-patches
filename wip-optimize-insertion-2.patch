

From: K.Takata <kentkt@csc.jp>


---
 src/change.c   |   18 +++++++++++++++---
 src/edit.c     |    9 +++++++++
 src/memline.c  |    7 ++++++-
 src/textprop.c |    5 +++++
 4 files changed, 35 insertions(+), 4 deletions(-)

diff --git a/src/change.c b/src/change.c
index f78e14515..55cfccfee 100644
--- a/src/change.c
+++ b/src/change.c
@@ -1309,9 +1309,21 @@ ins_char_bytes(char_u *buf, int charlen)
 	}
     }
 
-    newp = alloc(linelen + newlen - oldlen);
-    if (newp == NULL)
-	return;
+    int new_size = linelen + newlen - oldlen;
+    //Trace("ins_char_bytes(): ml_line_size: %d, new_size: %d", curbuf->b_ml.ml_line_size, new_size);
+    //Trace("ins_char_bytes(): ml_flags: %08x", curbuf->b_ml.ml_flags & (ML_LINE_DIRTY | ML_ALLOCATED));
+    if (/*(curbuf->b_ml.ml_flags & (ML_ALLOCATED)) ==*/ 0
+	    || (curbuf->b_ml.ml_line_size < new_size))
+    {
+	//Trace("ins_char_bytes(): oldlen: %d, newlen: %d, new_size: %d", oldlen, newlen, new_size);
+	new_size = new_size * 3 / 2;
+	newp = alloc(new_size);
+	if (newp == NULL)
+	    return;
+	curbuf->b_ml.ml_line_size = new_size;
+    }
+    else
+	newp = oldp;
 
     // Copy bytes before the cursor.
     if (col > 0)
diff --git a/src/edit.c b/src/edit.c
index 450a48e64..4c01d625f 100644
--- a/src/edit.c
+++ b/src/edit.c
@@ -447,6 +447,7 @@ edit(
      */
     for (;;)
     {
+	//Trace("for (;;), count=%d", count);
 #ifdef FEAT_RIGHTLEFT
 	if (!revins_legal)
 	    revins_scol = -1;	    // reset on illegal motions
@@ -509,6 +510,7 @@ edit(
 	}
 #endif
 
+	//Trace("edit(): 1");
 	/*
 	 * If we inserted a character at the last position of the last line in
 	 * the window, scroll the window one line up. This avoids an extra
@@ -563,6 +565,7 @@ edit(
 #endif
 		    set_topline(curwin, curwin->w_topline + 1);
 	    }
+	    //Trace("edit(): 2");
 	}
 
 	// May need to adjust w_topline to show the cursor.
@@ -571,8 +574,10 @@ edit(
 
 	did_backspace = FALSE;
 
+	//Trace("calling validate_cursor()");
 	if (count <= 1)
 	    validate_cursor();		// may set must_redraw
+	//Trace("validate_cursor() end");
 
 	/*
 	 * Redraw the display when no characters are waiting.
@@ -639,7 +644,9 @@ edit(
 
 	    do
 	    {
+		//Trace("calling safe_vgetc()");
 		c = safe_vgetc();
+		//Trace("safe_vgetc(), %c %02x", c, c);
 
 		if (stop_insert_mode
 #ifdef FEAT_TERMINAL
@@ -891,6 +898,7 @@ do_intr:
 		break;
 	    }
 doESCkey:
+	    //Trace("label doESCkey:");
 	    /*
 	     * This is the ONLY return from edit()!
 	     */
@@ -1398,6 +1406,7 @@ docomplete:
 #endif
 
 normalchar:
+	    //Trace("label normalchar:");
 	    /*
 	     * Insert a normal character.
 	     */
diff --git a/src/memline.c b/src/memline.c
index d6eddc210..88dea6c13 100644
--- a/src/memline.c
+++ b/src/memline.c
@@ -2795,6 +2795,7 @@ errorret:
 	buf->b_ml.ml_line_size = 0;
 	buf->b_ml.ml_line_textlen = buf->b_ml.ml_line_len;
 	buf->b_ml.ml_line_lnum = lnum;
+	buf->b_ml.ml_line_size = 0;
 	return questions;
     }
     if (lnum <= 0)			// pretend line 0 is line 1
@@ -2805,6 +2806,7 @@ errorret:
 	buf->b_ml.ml_line_len = 1;
 	buf->b_ml.ml_line_size = 0;
 	buf->b_ml.ml_line_textlen = buf->b_ml.ml_line_len;
+	buf->b_ml.ml_line_size = 0;
 	return (char_u *)"";
     }
 
@@ -2866,6 +2868,7 @@ errorret:
 #endif
 	    buf->b_ml.ml_line_textlen = buf->b_ml.ml_line_len;
 	buf->b_ml.ml_line_lnum = lnum;
+	buf->b_ml.ml_line_size = 0;
 	buf->b_ml.ml_flags &= ~(ML_LINE_DIRTY | ML_ALLOCATED);
     }
     if (will_change)
@@ -3707,11 +3710,13 @@ ml_replace_len(
 	{
 	    char_u *newline;
 	    size_t textproplen = curbuf->b_ml.ml_line_len - oldtextlen;
+	    size_t new_size = len + textproplen;
 
 	    // Need to copy over text properties, stored after the text.
-	    newline = alloc(len + textproplen);
+	    newline = alloc(new_size);
 	    if (newline != NULL)
 	    {
+		curbuf->b_ml.ml_line_size = new_size;
 		mch_memmove(newline, line, len);
 		mch_memmove(newline + len, curbuf->b_ml.ml_line_ptr
 						    + oldtextlen, textproplen);
diff --git a/src/textprop.c b/src/textprop.c
index 5d23c1673..19d92a4f5 100644
--- a/src/textprop.c
+++ b/src/textprop.c
@@ -903,6 +903,7 @@ prop_add_one(
 	}
 	buf->b_ml.ml_line_ptr = newtext;
 	buf->b_ml.ml_line_len = new_line_len;
+	buf->b_ml.ml_line_size = new_line_len;
 	buf->b_ml.ml_flags |= ML_LINE_DIRTY;
     }
 
@@ -1469,6 +1470,7 @@ set_text_props(linenr_T lnum, textprop_T *tps, int count)
 	}
 	curbuf->b_ml.ml_line_ptr = newtext;
 	curbuf->b_ml.ml_line_len = textlen;
+	curbuf->b_ml.ml_line_size = textlen;
 	curbuf->b_ml.ml_flags |= ML_LINE_DIRTY;
     }
     else
@@ -1642,6 +1644,7 @@ add_text_props(linenr_T lnum, textprop_T *text_props, int text_prop_count)
 	um.props[um.prop_count++] = text_props[i];
 
     um_close(&um);
+    // TODO: Update curbuf->b_ml.ml_line_size?
 }
 
 /*
@@ -1840,6 +1843,7 @@ f_prop_clear(typval_T *argvars, typval_T *rettv UNUSED)
 		did_clear = TRUE;
 		for (idx = um.prop_count - 1; idx >= 0; --idx)
 		    um_delete_prop(&um, idx);
+		// TODO: Update buf->b_ml.ml_line_size?
 	    }
 	}
 	um_close(&um);
@@ -3031,6 +3035,7 @@ adjust_prop_columns(
 	um_close(&um);
     else
 	um_abort(&um);
+    // TODO: Update curbuf->b_ml.ml_line_size?
     return dirty;
 }
 
