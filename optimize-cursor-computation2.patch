Signed-off-by: K.Takata <kentkt@csc.jp>

From: K.Takata <kentkt@csc.jp>


---
 src/charset.c         |   21 ++++++++++++++++++++-
 src/misc1.c           |    4 +++-
 src/proto/charset.pro |    1 +
 src/structs.h         |    1 +
 4 files changed, 25 insertions(+), 2 deletions(-)

diff --git a/src/charset.c b/src/charset.c
index de3657f54..a1b3e4ca5 100644
--- a/src/charset.c
+++ b/src/charset.c
@@ -936,7 +936,12 @@ win_linetabsize_cts(chartabsize_T *cts, colnr_T len)
     if (s_use_vcol_cache)
     {
 	if (slen == MAXCOL)
-	    slen = STRLEN(cts->cts_line);
+	{
+	    if (cts->cts_len != -1)
+		slen = cts->cts_len;
+	    else
+		slen = STRLEN(cts->cts_line);
+	}
 	col_save = (colnr_T)((varnumber_T)slen * 4 / 5);
 	if (slen - col_save > 4096)
 	    col_save = slen - 4096;
@@ -1145,12 +1150,26 @@ init_chartabsize_arg(
 	colnr_T		col,
 	char_u		*line,
 	char_u		*ptr)
+{
+    init_chartabsize_arg_len(cts, wp, lnum, col, line, ptr, -1);
+}
+
+    void
+init_chartabsize_arg_len(
+	chartabsize_T	*cts,
+	win_T		*wp,
+	linenr_T	lnum UNUSED,
+	colnr_T		col,
+	char_u		*line,
+	char_u		*ptr,
+	colnr_T		len)
 {
     CLEAR_POINTER(cts);
     cts->cts_win = wp;
     cts->cts_vcol = col;
     cts->cts_line = line;
     cts->cts_ptr = ptr;
+    cts->cts_len = len;
 #ifdef FEAT_LINEBREAK
     cts->cts_bri_size = -1;
 #endif
diff --git a/src/misc1.c b/src/misc1.c
index c4e6d30cf..cc27ced20 100644
--- a/src/misc1.c
+++ b/src/misc1.c
@@ -405,10 +405,12 @@ plines_win_nofold(win_T *wp, linenr_T lnum)
     char_u	*s;
     long	col;
     int		width;
+    colnr_T	len;
     chartabsize_T cts;
 
     s = ml_get_buf(wp->w_buffer, lnum, FALSE);
-    init_chartabsize_arg(&cts, wp, lnum, 0, s, s);
+    len = ml_get_buf_len(wp->w_buffer, lnum);
+    init_chartabsize_arg_len(&cts, wp, lnum, 0, s, s, len);
     if (*s == NUL
 #ifdef FEAT_PROP_POPUP
 	    && !cts.cts_has_prop_with_text
diff --git a/src/proto/charset.pro b/src/proto/charset.pro
index 02894b08e..88a4c61df 100644
--- a/src/proto/charset.pro
+++ b/src/proto/charset.pro
@@ -40,6 +40,7 @@ int vim_isfilec_or_wc(int c);
 int vim_isprintc(int c);
 int vim_isprintc_strict(int c);
 void init_chartabsize_arg(chartabsize_T *cts, win_T *wp, linenr_T lnum, colnr_T col, char_u *line, char_u *ptr);
+void init_chartabsize_arg_len(chartabsize_T *cts, win_T *wp, linenr_T lnum, colnr_T col, char_u *line, char_u *ptr, colnr_T len);
 void clear_chartabsize_arg(chartabsize_T *cts);
 int lbr_chartabsize(chartabsize_T *cts);
 int lbr_chartabsize_adv(chartabsize_T *cts);
diff --git a/src/structs.h b/src/structs.h
index 68d498404..f28041b9a 100644
--- a/src/structs.h
+++ b/src/structs.h
@@ -5289,6 +5289,7 @@ typedef struct {
 #endif
     int		cts_vcol;		// virtual column at current position
     int		cts_max_head_vcol;	// see win_lbr_chartabsize()
+    colnr_T	cts_len;		// length of cts_line, or -1
 } chartabsize_T;
 
 /*
