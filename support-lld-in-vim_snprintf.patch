# HG changeset patch
# Parent  0145d4d67201db02fba99cf43e73d6e398358ba0

diff --git a/src/json.c b/src/json.c
--- a/src/json.c
+++ b/src/json.c
@@ -104,8 +104,8 @@ json_encode_item(garray_T *gap, typval_T
 	    break;
 
 	case VAR_NUMBER:
-	    vim_snprintf((char *)numbuf, NUMBUFLEN, "%ld",
-						    (long)val->vval.v_number);
+	    vim_snprintf((char *)numbuf, NUMBUFLEN, "%lld",
+						    val->vval.v_number);
 	    ga_concat(gap, numbuf);
 	    break;
 
diff --git a/src/message.c b/src/message.c
--- a/src/message.c
+++ b/src/message.c
@@ -4310,7 +4310,11 @@ vim_snprintf(str, str_m, fmt, a1, a2, a3
 		if (length_modifier == 'l' && *p == 'l')
 		{
 		    /* double l = long long */
+#ifdef FEAT_NUM64
+		    length_modifier = 'L';
+#else
 		    length_modifier = 'l';	/* treat it as a single 'l' */
+#endif
 		    p++;
 		}
 	    }
@@ -4447,6 +4451,12 @@ vim_snprintf(str, str_m, fmt, a1, a2, a3
 		    long int long_arg = 0;
 		    unsigned long int ulong_arg = 0;
 
+#ifdef FEAT_NUM64
+		    /* only defined for length modifier ll */
+		    varnumber_T llong_arg = 0;
+		    uvarnumber_T ullong_arg = 0;
+#endif
+
 		    /* pointer argument value -only defined for p
 		     * conversion */
 		    void *ptr_arg = NULL;
@@ -4503,6 +4513,23 @@ vim_snprintf(str, str_m, fmt, a1, a2, a3
 			    else if (long_arg < 0)
 				arg_sign = -1;
 			    break;
+#ifdef FEAT_NUM64
+			case 'L':
+			    llong_arg =
+# ifndef HAVE_STDARG_H
+					get_a_arg(arg_idx);
+# else
+#  if defined(FEAT_EVAL)
+					tvs != NULL ? tv_nr(tvs, &arg_idx) :
+#  endif
+					    va_arg(ap, varnumber_T);
+# endif
+			    if (llong_arg > 0)
+				arg_sign =  1;
+			    else if (llong_arg < 0)
+				arg_sign = -1;
+			    break;
+#endif
 			}
 		    }
 		    else
@@ -4539,6 +4566,22 @@ vim_snprintf(str, str_m, fmt, a1, a2, a3
 				if (ulong_arg != 0)
 				    arg_sign = 1;
 				break;
+#ifdef FEAT_NUM64
+			    case 'L':
+				ullong_arg =
+# ifndef HAVE_STDARG_H
+					    get_a_arg(arg_idx);
+# else
+#  if defined(FEAT_EVAL)
+					    tvs != NULL ? (uvarnumber_T)
+							tv_nr(tvs, &arg_idx) :
+#  endif
+						va_arg(ap, uvarnumber_T);
+# endif
+				if (ullong_arg != 0)
+				    arg_sign = 1;
+				break;
+#endif
 			}
 		    }
 
@@ -4583,17 +4626,27 @@ vim_snprintf(str, str_m, fmt, a1, a2, a3
 		    }
 		    else
 		    {
-			char	f[5];
+			char	f[6];
 			int	f_l = 0;
 
 			/* construct a simple format string for sprintf */
 			f[f_l++] = '%';
 			if (!length_modifier)
 			    ;
-			else if (length_modifier == '2')
+			else if (length_modifier == 'L')
 			{
+#ifdef FEAT_NUM64
+# ifdef WIN3264
+			    f[f_l++] = 'I';
+			    f[f_l++] = '6';
+			    f[f_l++] = '4';
+# else
 			    f[f_l++] = 'l';
 			    f[f_l++] = 'l';
+# endif
+#else
+			    f[f_l++] = 'l';
+#endif
 			}
 			else
 			    f[f_l++] = length_modifier;
@@ -4614,6 +4667,11 @@ vim_snprintf(str, str_m, fmt, a1, a2, a3
 			    case 'l': str_arg_l += sprintf(
 						tmp + str_arg_l, f, long_arg);
 				      break;
+#ifdef FEAT_NUM64
+			    case 'L': str_arg_l += sprintf(
+					       tmp + str_arg_l, f, llong_arg);
+				      break;
+#endif
 			    }
 			}
 			else
@@ -4628,6 +4686,11 @@ vim_snprintf(str, str_m, fmt, a1, a2, a3
 			    case 'l': str_arg_l += sprintf(
 					       tmp + str_arg_l, f, ulong_arg);
 				      break;
+#ifdef FEAT_NUM64
+			    case 'L': str_arg_l += sprintf(
+					      tmp + str_arg_l, f, ullong_arg);
+				      break;
+#endif
 			    }
 			}
 
