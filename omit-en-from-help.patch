# HG changeset patch
# Parent  fa2e1a10f7e8a99905937ce5724774836b04fb83

diff --git a/runtime/doc/helphelp.txt b/runtime/doc/helphelp.txt
--- a/runtime/doc/helphelp.txt
+++ b/runtime/doc/helphelp.txt
@@ -257,8 +257,8 @@ The second one finds the English user ma
 "it".
 
 When using command-line completion for the ":help" command, the "@en"
-extension is only shown when a tag exists for multiple languages.  When the
-tag only exists for English "@en" is omitted.
+extension is always omitted even when a tag exists for multiple languages.
+You need to add "@en" explicitly when you want to find the tag for English.
 
 When using |CTRL-]| or ":help!" in a non-English help file Vim will try to
 find the tag in the same language.  If not found then 'helplang' will be used
diff --git a/src/ex_getln.c b/src/ex_getln.c
--- a/src/ex_getln.c
+++ b/src/ex_getln.c
@@ -4499,7 +4499,7 @@ static void	cleanup_help_tags(int num_fi
     static void
 cleanup_help_tags(int num_file, char_u **file)
 {
-    int		i, j;
+    int		i;
     int		len;
 
     for (i = 0; i < num_file; ++i)
@@ -4507,15 +4507,8 @@ cleanup_help_tags(int num_file, char_u *
 	len = (int)STRLEN(file[i]) - 3;
 	if (len > 0 && STRCMP(file[i] + len, "@en") == 0)
 	{
-	    /* Sorting on priority means the same item in another language may
-	     * be anywhere.  Search all items for a match up to the "@en". */
-	    for (j = 0; j < num_file; ++j)
-		if (j != i
-			&& (int)STRLEN(file[j]) == len + 3
-			&& STRNCMP(file[i], file[j], len + 1) == 0)
-		    break;
-	    if (j == num_file)
-		file[i][len] = NUL;
+	    /* Always omit "@en". */
+	    file[i][len] = NUL;
 	}
     }
 }
