# HG changeset patch
# Parent  1856271fb9299fff270ee3fc540aa631892db4b2

diff --git a/src/change.c b/src/change.c
--- a/src/change.c
+++ b/src/change.c
@@ -1181,9 +1181,11 @@ ins_char_bytes(char_u *buf, int charlen)
     void
 ins_str(char_u *s)
 {
+    static char_u	*prevp = NULL;
     char_u	*oldp, *newp;
     int		newlen = (int)STRLEN(s);
     int		oldlen;
+    int		new_size;
     colnr_T	col;
     linenr_T	lnum = curwin->w_cursor.lnum;
 
@@ -1194,14 +1196,37 @@ ins_str(char_u *s)
     oldp = ml_get(lnum);
     oldlen = (int)ml_get_len(lnum);
 
-    newp = alloc(oldlen + newlen + 1);
-    if (newp == NULL)
-	return;
-    if (col > 0)
+    new_size = oldlen + newlen + 1;
+    if ((curbuf->b_ml.ml_line_size < new_size) || (prevp != oldp)
+#ifdef FEAT_PROP_POPUP
+	    || curbuf->b_has_textprop
+#endif
+	    )
+    {
+	new_size = new_size * 3 / 2;
+	newp = alloc(new_size);
+	if (newp == NULL)
+	    return;
+	curbuf->b_ml.ml_line_size = new_size;
+    }
+    else
+	newp = oldp;
+    prevp = newp;
+
+    if (col > 0 && newp != oldp)
 	mch_memmove(newp, oldp, (size_t)col);
+    mch_memmove(newp + col + newlen, oldp + col, (size_t)(oldlen - col + 1));
     mch_memmove(newp + col, s, (size_t)newlen);
-    mch_memmove(newp + col + newlen, oldp + col, (size_t)(oldlen - col + 1));
-    ml_replace(lnum, newp, FALSE);
+    if (newp != oldp)
+	ml_replace_len(lnum, newp, oldlen + newlen, FALSE, FALSE);
+    else
+    {
+	curbuf->b_ml.ml_line_len = oldlen + newlen + 1;
+	curbuf->b_ml.ml_line_textlen = oldlen + newlen + 1;
+	curbuf->b_ml.ml_line_lnum = lnum;
+	curbuf->b_ml.ml_flags =
+			(curbuf->b_ml.ml_flags | ML_LINE_DIRTY) & ~ML_EMPTY;
+    }
     inserted_bytes(lnum, col, newlen);
     curwin->w_cursor.col += newlen;
 }
diff --git a/src/memline.c b/src/memline.c
--- a/src/memline.c
+++ b/src/memline.c
@@ -2840,6 +2840,7 @@ errorret:
 	// make sure the text is in allocated memory
 	if (p != NULL)
 	{
+	    buf->b_ml.ml_line_size = buf->b_ml.ml_line_len;
 	    memmove(p, buf->b_ml.ml_line_ptr, buf->b_ml.ml_line_len);
 	    buf->b_ml.ml_line_ptr = p;
 	    buf->b_ml.ml_flags |= ML_ALLOCATED;
diff --git a/src/structs.h b/src/structs.h
--- a/src/structs.h
+++ b/src/structs.h
@@ -802,6 +802,7 @@ typedef struct memline
 #define ML_ALLOCATED	0x10	// ml_line_ptr is an allocated copy
     int		ml_flags;
 
+    int		ml_line_size;   // allocated size of ml_line_ptr (Used for ins_str())
     colnr_T	ml_line_len;	// length of the cached line + NUL + text properties
     colnr_T	ml_line_textlen;// length of the cached line + NUL, 0 if not known yet
     linenr_T	ml_line_lnum;	// line number of cached line, 0 if not valid
