# HG changeset patch
# Parent  4166b3b9d61e2397195c1b14bae89b519ef3f839

diff --git a/src/locale.c b/src/locale.c
--- a/src/locale.c
+++ b/src/locale.c
@@ -248,6 +248,7 @@ init_locale(void)
 #  endif
 	// expand_env() doesn't work yet, because g_chartab[] is not
 	// initialized yet, call vim_getenv() directly
+	OutputDebugString("init_locale");
 	p = vim_getenv((char_u *)"VIMRUNTIME", &mustfree);
 	if (p != NULL && *p != NUL)
 	{
diff --git a/src/misc1.c b/src/misc1.c
--- a/src/misc1.c
+++ b/src/misc1.c
@@ -1261,6 +1261,32 @@ free_users(void)
 }
 #endif
 
+#if defined(MSWIN) || defined(PROTO)
+/*
+ * Initilize $VIM and $VIMRUNTIME when 'enc' is updated.
+ */
+    void
+init_vimdir(void)
+{
+    int	    mustfree;
+    char_u  *p;
+
+    mch_get_exe_name();
+
+    mustfree = FALSE;
+    didset_vim = FALSE;
+    p = vim_getenv((char_u *)"VIM", &mustfree);
+    if (mustfree)
+	vim_free(p);
+
+    mustfree = FALSE;
+    didset_vimruntime = FALSE;
+    p = vim_getenv((char_u *)"VIMRUNTIME", &mustfree);
+    if (mustfree)
+	vim_free(p);
+}
+#endif
+
 /*
  * Call expand_env() and store the result in an allocated string.
  * This is not very memory efficient, this expects the result to be freed
@@ -1406,7 +1432,9 @@ expand_env_esc(
 			++tail;
 #endif
 		    *var = NUL;
+		    Trace("expanding: %s", dst);
 		    var = vim_getenv(dst, &mustfree);
+		    Trace("expanded: %s", var);
 #if defined(MSWIN) || defined(UNIX)
 		}
 #endif
@@ -1693,6 +1721,7 @@ vim_getenv(char_u *name, int *mustfree)
     if (!vimruntime && STRCMP(name, "VIM") != 0)
 	return NULL;
 
+Trace("vim_getenv: $VIM or $VIMRUNTIME");
     /*
      * When expanding $VIMRUNTIME fails, try using $VIM/vim<version> or $VIM.
      * Don't do this when default_vimruntime_dir is non-empty.
@@ -1750,10 +1779,12 @@ vim_getenv(char_u *name, int *mustfree)
 	else
 	    p = exe_name;
 #endif
+Trace("vim_getenv: p=%s", p);
 	if (p != NULL)
 	{
 	    // remove the file name
 	    pend = gettail(p);
+Trace("vim_getenv: pend=%s", pend);
 
 	    // remove "doc/" from 'helpfile', if present
 	    if (p == p_hf)
@@ -1792,6 +1823,7 @@ vim_getenv(char_u *name, int *mustfree)
 		pend = remove_tail(p, pend, (char_u *)RUNTIME_DIRNAME);
 		pend = remove_tail(p, pend, (char_u *)VIM_VERSION_NODOT);
 	    }
+Trace("vim_getenv: pend2=%s", pend);
 
 	    // remove trailing path separator
 	    if (pend > p && after_pathsep(p, pend))
@@ -1853,14 +1885,17 @@ vim_getenv(char_u *name, int *mustfree)
 	if (vimruntime)
 	{
 	    vim_setenv((char_u *)"VIMRUNTIME", p);
+Trace("vim_getenv: VIMRUNTIME=%s", p);
 	    didset_vimruntime = TRUE;
 	}
 	else
 	{
 	    vim_setenv((char_u *)"VIM", p);
+Trace("vim_getenv: VIM=%s", p);
 	    didset_vim = TRUE;
 	}
     }
+Trace("vim_getenv: end");
     return p;
 }
 
diff --git a/src/option.c b/src/option.c
--- a/src/option.c
+++ b/src/option.c
@@ -2302,6 +2302,7 @@ option_expand(int opt_idx, char_u *val)
 	    (char_u **)options[opt_idx].var == &p_sps ? (char_u *)"file:" :
 #endif
 				  NULL);
+if ((char_u **)options[opt_idx].var == &p_hf) Trace("option_expand: %s", NameBuff);
     if (STRCMP(NameBuff, val) == 0)   // they are the same
 	return NULL;
 
diff --git a/src/optionstr.c b/src/optionstr.c
--- a/src/optionstr.c
+++ b/src/optionstr.c
@@ -1037,9 +1037,13 @@ ambw_end:
 	    }
 
 #if defined(MSWIN)
-	    // $HOME may have characters in active code page.
+	    // $HOME, $VIM and $VIMRUNTIME may have characters in active code
+	    // page.
 	    if (varp == &p_enc)
+	    {
 		init_homedir();
+		init_vimdir();
+	    }
 #endif
 	}
     }
diff --git a/src/os_win32.c b/src/os_win32.c
--- a/src/os_win32.c
+++ b/src/os_win32.c
@@ -237,7 +237,7 @@ static void restore_console_color_rgb(vo
 static int suppress_winsize = 1;	// don't fiddle with console
 #endif
 
-static char_u *exe_path = NULL;
+static WCHAR *exe_pathw = NULL;
 
 static BOOL win8_or_later = FALSE;
 
@@ -438,26 +438,45 @@ wait_for_single_object(
 # endif
 #endif
 
-    static void
-get_exe_name(void)
+    void
+mch_get_exe_name(void)
 {
     // Maximum length of $PATH is more than MAXPATHL.  8191 is often mentioned
     // as the maximum length that works (plus a NUL byte).
 #define MAX_ENV_PATH_LEN 8192
     char	temp[MAX_ENV_PATH_LEN];
     char_u	*p;
-
-    if (exe_name == NULL)
+    WCHAR	buf[MAX_PATH];
+    int		updated = FALSE;
+    static int	enc_prev = -1;
+
+    if (exe_name == NULL || exe_pathw == NULL || enc_prev != enc_codepage)
     {
 	// store the name of the executable, may be used for $VIM
-	GetModuleFileName(NULL, temp, MAX_ENV_PATH_LEN - 1);
-	if (*temp != NUL)
-	    exe_name = FullName_save((char_u *)temp, FALSE);
-    }
-
-    if (exe_path == NULL && exe_name != NULL)
-    {
-	exe_path = vim_strnsave(exe_name, gettail_sep(exe_name) - exe_name);
+	GetModuleFileNameW(NULL, buf, MAX_PATH);
+	if (*buf != NUL)
+	{
+	    WCHAR	*wp;
+	    if (enc_codepage == -1)
+		enc_codepage = GetACP();
+	    exe_name = utf16_to_enc(buf, NULL);
+	    enc_prev = enc_codepage;
+
+	    wp = wcsrchr(buf, '\\');
+	    if (wp != NULL)
+		*wp = NUL;
+	    if (exe_pathw != NULL)
+		vim_free(exe_pathw);
+	    exe_pathw = _wcsdup(buf);
+	    updated = TRUE;
+OutputDebugString(exe_name);
+	}
+    }
+
+    if (exe_pathw != NULL && updated)
+    {
+	char_u	*exe_path = utf16_to_enc(exe_pathw, NULL);
+
 	if (exe_path != NULL)
 	{
 	    // Append our starting directory to $PATH, so that when doing
@@ -477,6 +496,7 @@ get_exe_name(void)
 		STRCAT(temp, exe_path);
 		vim_setenv((char_u *)"PATH", (char_u *)temp);
 	    }
+	    vim_free(exe_path);
 	}
     }
 }
@@ -514,9 +534,9 @@ vimLoadLib(char *name)
 
     // NOTE: Do not use mch_dirname() and mch_chdir() here, they may call
     // vimLoadLib() recursively, which causes a stack overflow.
-    if (exe_path == NULL)
-	get_exe_name();
-    if (exe_path != NULL)
+    if (exe_pathw == NULL)
+	mch_get_exe_name();
+    if (exe_pathw != NULL)
     {
 	WCHAR old_dirw[MAXPATHL];
 
@@ -525,7 +545,7 @@ vimLoadLib(char *name)
 	    // Change directory to where the executable is, both to make
 	    // sure we find a .dll there and to avoid looking for a .dll
 	    // in the current directory.
-	    SetCurrentDirectory((LPCSTR)exe_path);
+	    SetCurrentDirectoryW(exe_pathw);
 	    dll = LoadLibrary(name);
 	    SetCurrentDirectoryW(old_dirw);
 	    return dll;
@@ -2962,7 +2982,7 @@ mch_check_win(
     int argc UNUSED,
     char **argv UNUSED)
 {
-    get_exe_name();
+    mch_get_exe_name();
 
 #if defined(FEAT_GUI_MSWIN) && !defined(VIMDLL)
     return OK;	    // GUI always has a tty
diff --git a/src/proto/misc1.pro b/src/proto/misc1.pro
--- a/src/proto/misc1.pro
+++ b/src/proto/misc1.pro
@@ -25,6 +25,7 @@ void vim_beep(unsigned val);
 void init_homedir(void);
 void free_homedir(void);
 void free_users(void);
+void init_vimdir(void);
 char_u *expand_env_save(char_u *src);
 char_u *expand_env_save_opt(char_u *src, int one);
 void expand_env(char_u *src, char_u *dst, int dstlen);
diff --git a/src/proto/os_win32.pro b/src/proto/os_win32.pro
--- a/src/proto/os_win32.pro
+++ b/src/proto/os_win32.pro
@@ -1,4 +1,5 @@
 /* os_win32.c */
+void mch_get_exe_name(void);
 HINSTANCE vimLoadLib(char *name);
 int mch_is_gui_executable(void);
 HINSTANCE find_imported_module_by_funcname(HINSTANCE hInst, const char *funcname);
