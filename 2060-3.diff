# HG changeset patch
# Parent  6fbbbb0693ff49d687c0fba1fa94251ffe811ec7

diff --git a/src/os_win32.c b/src/os_win32.c
--- a/src/os_win32.c
+++ b/src/os_win32.c
@@ -207,7 +207,7 @@ static int win32_getattrs(char_u *name);
 static int win32_setattrs(char_u *name, int attrs);
 static int win32_set_archive(char_u *name);
 
-#if !defined(FEAT_GUI)
+#ifndef FEAT_GUI_W32
 static int has_vtp = 0;
 static void vtp_init();
 static void vtp_exit();
@@ -218,14 +218,12 @@ static void vtp_sgr_bulks(int argc, int 
 static guicolor_T save_console_bg_rgb;
 static guicolor_T save_console_fg_rgb;
 
-#ifdef FEAT_TERMGUICOLORS
-# define USE_VTP		(has_vtp && p_tgc)
-#else
-# define USE_VTP		0
-#endif
-
-#endif
-#ifndef FEAT_GUI_W32
+# ifdef FEAT_TERMGUICOLORS
+#  define USE_VTP		(has_vtp && p_tgc)
+# else
+#  define USE_VTP		0
+# endif
+
 static void set_console_color_rgb(void);
 static void reset_console_color_rgb(void);
 #endif
@@ -243,7 +241,7 @@ static char_u *exe_path = NULL;
 
 static BOOL win8_or_later = FALSE;
 
-#ifndef FEAT_GUI
+#ifndef FEAT_GUI_W32
 /* Dynamic loading for portability */
 typedef struct _DYN_CONSOLE_SCREEN_BUFFER_INFOEX
 {
@@ -291,9 +289,8 @@ get_build_number(
     }
     return ver;
 }
-#endif
-
-#ifndef FEAT_GUI_W32
+
+
 /*
  * Version of ReadConsoleInput() that works with IME.
  * Works around problems on Windows 8.
@@ -7537,8 +7534,7 @@ mch_setenv(char *var, char *value, int x
 # define VTP_FIRST_SUPPORT_BUILD MAKE_VER(10, 0, 15063)
 
     static void
-vtp_init(
-    void)
+vtp_init(void)
 {
     DWORD   ver, mode;
     HMODULE hKerneldll;
@@ -7576,8 +7572,7 @@ vtp_init(
 }
 
     static void
-vtp_exit(
-    void)
+vtp_exit(void)
 {
     reset_console_color_rgb();
 }
@@ -7635,8 +7630,7 @@ vtp_sgr_bulks(
 }
 
     static void
-set_console_color_rgb(
-    void)
+set_console_color_rgb(void)
 {
 # ifdef FEAT_TERMGUICOLORS
     DYN_CONSOLE_SCREEN_BUFFER_INFOEX csbi;
@@ -7669,8 +7663,7 @@ set_console_color_rgb(
 }
 
     static void
-reset_console_color_rgb(
-    void)
+reset_console_color_rgb(void)
 {
 # ifdef FEAT_TERMGUICOLORS
     DYN_CONSOLE_SCREEN_BUFFER_INFOEX csbi;
@@ -7690,8 +7683,7 @@ reset_console_color_rgb(
 }
 
     void
-control_console_color_rgb(
-    void)
+control_console_color_rgb(void)
 {
     if (USE_VTP)
 	set_console_color_rgb();
@@ -7700,8 +7692,7 @@ control_console_color_rgb(
 }
 
     int
-vtp_get_stat(
-    void)
+vtp_get_stat(void)
 {
     return has_vtp;
 }
