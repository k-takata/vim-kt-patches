# HG changeset patch
# Parent  94d22505d6ff1f029cc96c9c37c5094bd4be9ad5

diff --git a/src/evalfunc.c b/src/evalfunc.c
--- a/src/evalfunc.c
+++ b/src/evalfunc.c
@@ -6456,25 +6456,52 @@ f_has(typval_T *argvars, typval_T *rettv
     {
 	if (STRNICMP(name, "patch", 5) == 0)
 	{
+	    char    *end;
+
 	    x = TRUE;
 	    if (name[5] == '-'
-		    && STRLEN(name) >= 11
-		    && vim_isdigit(name[6])
-		    && vim_isdigit(name[8])
-		    && vim_isdigit(name[10]))
+		    && STRLEN(name) >= 11	// "patch-x.y.z"
+		    && vim_isdigit(name[6]))
 	    {
-		int major = atoi((char *)name + 6);
-		int minor = atoi((char *)name + 8);
-
-		// Expect "patch-9.9.01234".
-		n = (major < VIM_VERSION_MAJOR
-		     || (major == VIM_VERSION_MAJOR
-			 && (minor < VIM_VERSION_MINOR
-			     || (minor == VIM_VERSION_MINOR
-				 && has_patch(atoi((char *)name + 10))))));
+		int major, minor, patch = -1;
+
+		n = TRUE;
+
+		major = (int)strtoul((char *)name + 6, &end, 10);
+		if (*end != '.' || !vim_isdigit(end[1]))
+		    n = FALSE;
+		else
+		{
+		    minor = (int)strtoul(end + 1, &end, 10);
+		    if (*end != '.' || !vim_isdigit(end[1]))
+			n = FALSE;
+		    else
+		    {
+			patch = (int)strtoul(end + 1, &end, 10);
+			if (*end != '\0')
+			    n = FALSE;
+		    }
+		}
+
+		if (n && (patch >= 0))
+		    // Expect "patch-9.9.01234".
+		    n = (major < VIM_VERSION_MAJOR
+			 || (major == VIM_VERSION_MAJOR
+			     && (minor < VIM_VERSION_MINOR
+				 || (minor == VIM_VERSION_MINOR
+				     && has_patch(patch)))));
+	    }
+	    else if (vim_isdigit(name[5]))	// "patch123"
+	    {
+		int patch = (int)strtoul((char *)name + 5, &end, 10);
+
+		if (*end == '\0')
+		    n = has_patch(patch);
+		else
+		    n = FALSE;
 	    }
 	    else
-		n = has_patch(atoi((char *)name + 5));
+		n = FALSE;
 	}
 	else if (STRICMP(name, "vim_starting") == 0)
 	{
diff --git a/src/testdir/test_expr.vim b/src/testdir/test_expr.vim
--- a/src/testdir/test_expr.vim
+++ b/src/testdir/test_expr.vim
@@ -35,12 +35,20 @@ func Test_version()
   call assert_true(has('patch-6.9.999'))
   call assert_true(has('patch-7.1.999'))
   call assert_true(has('patch-7.4.123'))
+  call assert_true(has('patch-09.00.0'))
 
   call assert_false(has('patch-7'))
   call assert_false(has('patch-7.4'))
   call assert_false(has('patch-7.4.'))
   call assert_false(has('patch-9.1.0'))
   call assert_false(has('patch-9.9.1'))
+
+  call assert_false(has('patchX'))
+  call assert_false(has('patch0X'))
+  call assert_false(has('patch-X'))
+  call assert_false(has('patch-9.0.0X'))
+  call assert_false(has('patch-9X0X0'))
+  call assert_false(has('patch-9-0-0'))
 endfunc
 
 func Test_op_ternary()
