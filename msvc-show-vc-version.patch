# HG changeset patch
# Parent  edb7e0c8693583b694649afb1a5b9d4f78af3d24

diff --git a/src/msvc-latest.bat b/src/msvc-latest.bat
--- a/src/msvc-latest.bat
+++ b/src/msvc-latest.bat
@@ -61,6 +61,11 @@ if exist "%InstallDir%\VC\Auxiliary\Buil
 )
 
 :done
+if defined VCToolsVersion (
+	if "%VSVEROPT%"=="-latest" (
+		title VCTools %VCToolsVersion:~0,5% ^(%VSCMD_ARG_TGT_ARCH%^)
+	)
+)
 if "%VSWHERE_SET%"=="yes" (
 	set VSWHERE=
 	set VSWHERE_SET=
diff --git a/src/msvc2015.bat b/src/msvc2015.bat
--- a/src/msvc2015.bat
+++ b/src/msvc2015.bat
@@ -12,4 +12,12 @@ rem   If you use Community (or Professio
 rem   option:
 rem     msvc2015 x64
 
+set Platform=
 call "%VS140COMNTOOLS%..\..\VC\vcvarsall.bat" %*
+if defined VisualStudioVersion (
+	if defined Platform (
+		title VC++ 2015 ^(%VisualStudioVersion%^) ^(%Platform%^)
+	) else (
+		title VC++ 2015 ^(%VisualStudioVersion%^) ^(x86^)
+	)
+)
diff --git a/src/msvc2017.bat b/src/msvc2017.bat
--- a/src/msvc2017.bat
+++ b/src/msvc2017.bat
@@ -11,3 +11,6 @@ rem     msvc2017 x86_amd64
 set "VSVEROPT=-version [15.0^,16.0^)"
 call "%~dp0msvc-latest.bat" %*
 set VSVEROPT=
+if defined VCToolsVersion (
+	title VC++ 2017 ^(%VCToolsVersion:~0,5%^) ^(%VSCMD_ARG_TGT_ARCH%^)
+)
diff --git a/src/msvc2019.bat b/src/msvc2019.bat
--- a/src/msvc2019.bat
+++ b/src/msvc2019.bat
@@ -11,3 +11,6 @@ rem     msvc2019 x64
 set "VSVEROPT=-version [16.0^,17.0^)"
 call "%~dp0msvc-latest.bat" %*
 set VSVEROPT=
+if defined VCToolsVersion (
+	title VC++ 2019 ^(%VCToolsVersion:~0,5%^) ^(%VSCMD_ARG_TGT_ARCH%^)
+)
diff --git a/src/msvc2022.bat b/src/msvc2022.bat
--- a/src/msvc2022.bat
+++ b/src/msvc2022.bat
@@ -11,3 +11,6 @@ rem     msvc2022 x64
 set "VSVEROPT=-version [17.0^,18.0^)"
 call "%~dp0msvc-latest.bat" %*
 set VSVEROPT=
+if defined VCToolsVersion (
+	title VC++ 2022 ^(%VCToolsVersion:~0,5%^) ^(%VSCMD_ARG_TGT_ARCH%^)
+)
