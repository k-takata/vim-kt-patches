# HG changeset patch
# Parent 934e9726a934fdfac82be80e01984cea16a17298
diff --git a/src/fileio.c b/src/fileio.c
--- a/src/fileio.c
+++ b/src/fileio.c
@@ -7073,6 +7073,7 @@
     {
 	/* Reload the buffer. */
 	buf_reload(buf, orig_mode);
+	do_modelines(0);
 #ifdef FEAT_PERSISTENT_UNDO
 	if (buf->b_p_udf && buf->b_ffname != NULL)
 	{
