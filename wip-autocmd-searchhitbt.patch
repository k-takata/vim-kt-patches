# HG changeset patch
# Parent  731d5fe2b0e8d4a929245417927a350872df8e98

diff --git a/runtime/doc/autocmd.txt b/runtime/doc/autocmd.txt
--- a/runtime/doc/autocmd.txt
+++ b/runtime/doc/autocmd.txt
@@ -415,6 +415,9 @@ Name			triggered by ~
 |TextChangedT|		after a change was made to the text in Terminal mode
 |TextYankPost|		after text has been yanked or deleted
 
+|SearchHitBottom|	when searching hit the bottom of the file
+|SearchHitTop|		when searching hit the top of the file
+
 |SafeState|		nothing pending, going to wait for the user to type a
 			character
 |SafeStateAgain|	repeated SafeState
@@ -1123,6 +1126,20 @@ SafeStateAgain			Like SafeState but afte
 				triggered often, don't do something that takes
 				time.
 
+							*SearchHitBottom*
+SearchHitBottom			When searching hit the bottom of the file.
+							*SearchHitTop*
+SearchHitTop			When searching hit the top of the file.
+				This is a simple example to show a popup when
+				hitting the top or the bottom: >
+	augroup SearchHit
+	  au!
+	  au SearchHitTop * call popup_create(
+		\ "Search hit top: " .. @/, #{border: [], time: 3000})
+	  au SearchHitBottom * call popup_create(
+		\ "Search hit bottom: " .. @/, #{border: [], time: 3000})
+	augroup END
+<
 							*SessionLoadPost*
 SessionLoadPost			After loading the session file created using
 				the |:mksession| command.
diff --git a/runtime/doc/tags b/runtime/doc/tags
--- a/runtime/doc/tags
+++ b/runtime/doc/tags
@@ -5700,6 +5700,8 @@ SQLGetType	ft_sql.txt	/*SQLGetType*
 SQLSetType	ft_sql.txt	/*SQLSetType*
 SafeState	autocmd.txt	/*SafeState*
 SafeStateAgain	autocmd.txt	/*SafeStateAgain*
+SearchHitBottom	autocmd.txt	/*SearchHitBottom*
+SearchHitTop	autocmd.txt	/*SearchHitTop*
 Select	visual.txt	/*Select*
 Select-mode	visual.txt	/*Select-mode*
 Select-mode-mapping	visual.txt	/*Select-mode-mapping*
diff --git a/src/autocmd.c b/src/autocmd.c
--- a/src/autocmd.c
+++ b/src/autocmd.c
@@ -164,6 +164,8 @@ static keyvalue_T event_tab[] = {
     KEYVALUE_ENTRY(EVENT_REMOTEREPLY, "RemoteReply"),
     KEYVALUE_ENTRY(EVENT_SAFESTATE, "SafeState"),
     KEYVALUE_ENTRY(EVENT_SAFESTATEAGAIN, "SafeStateAgain"),
+    KEYVALUE_ENTRY(EVENT_SEARCHHITBOTTOM, "SearchHitBottom"),
+    KEYVALUE_ENTRY(EVENT_SEARCHHITTOP, "SearchHitTop"),
     KEYVALUE_ENTRY(EVENT_SESSIONLOADPOST, "SessionLoadPost"),
     KEYVALUE_ENTRY(EVENT_SESSIONWRITEPOST, "SessionWritePost"),
     KEYVALUE_ENTRY(EVENT_SHELLCMDPOST, "ShellCmdPost"),
diff --git a/src/search.c b/src/search.c
--- a/src/search.c
+++ b/src/search.c
@@ -1129,9 +1129,16 @@ searchit(
 	     * written.
 	     */
 	    if (dir == BACKWARD)    // start second loop at the other end
+	    {
 		lnum = buf->b_ml.ml_line_count;
+		apply_autocmds(EVENT_SEARCHHITTOP, NULL, NULL, FALSE, curbuf);
+	    }
 	    else
+	    {
 		lnum = 1;
+		apply_autocmds(EVENT_SEARCHHITBOTTOM, NULL, NULL, FALSE,
+								      curbuf);
+	    }
 	    if (!shortmess(SHM_SEARCH)
 		    && shortmess(SHM_SEARCHCOUNT)
 		    && (options & SEARCH_MSG))
diff --git a/src/vim.h b/src/vim.h
--- a/src/vim.h
+++ b/src/vim.h
@@ -1411,6 +1411,8 @@ enum auto_event
     EVENT_REMOTEREPLY,		// upon string reception from a remote vim
     EVENT_SAFESTATE,		// going to wait for a character
     EVENT_SAFESTATEAGAIN,	// still waiting for a character
+    EVENT_SEARCHHITBOTTOM,	// hit bottom when searching
+    EVENT_SEARCHHITTOP,		// hit top when searching
     EVENT_SESSIONLOADPOST,	// after loading a session file
     EVENT_SESSIONWRITEPOST,	// after writing a session file
     EVENT_SHELLCMDPOST,		// after ":!cmd"
