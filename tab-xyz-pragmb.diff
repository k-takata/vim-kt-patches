changeset:   5944:5bd18445ddd7
tag:         tip
user:        Nathaniel Braun <pragmb@gmail.com>
date:        Thu Feb 27 10:00:00 2014 +0100
files:       runtime/doc/options.txt src/globals.h src/message.c src/option.c src/screen.c
description:
Add optional third tab character in "listchars"


diff --git a/runtime/doc/options.txt b/runtime/doc/options.txt
--- a/runtime/doc/options.txt
+++ b/runtime/doc/options.txt
@@ -4665,11 +4665,25 @@ A jump table for the options with a shor
 			omitted, there is no extra character at the end of the
 			line.
 	  						*lcs-tab*
-	  tab:xy	Two characters to be used to show a tab.  The first
-			char is used once.  The second char is repeated to
-			fill the space that the tab normally occupies.
+	  tab:xy[z]	Two or three characters to be used to show a tab.
+			The third character is optional.
+
+			If the tab is at least two characters wide then:
+			 - the first character is used once at the beginning
+			 - the second character is repeated to fill the tab
+			 - if the third character is specified, it is used
+			   once at the end
+
+			If the tab is one character wide then:
+			 - if the third character is specified, then
+			   the third character is used
+			 - if the third character is not specified, then
+			   the first character is used
+
 			"tab:>-" will show a tab that takes four spaces as
-			">---".  When omitted, a tab is show as ^I.
+			">---".  "tab:<->" will show a tab as that takes
+			four spaces as "<-->".  When omitted, a tab is shown
+			as ^I.
 	  						*lcs-trail*
 	  trail:c	Character to show for trailing spaces.  When omitted,
 			trailing spaces are blank.
diff --git a/src/globals.h b/src/globals.h
--- a/src/globals.h
+++ b/src/globals.h
@@ -1164,6 +1164,7 @@ EXTERN int	lcs_prec INIT(= NUL);
 EXTERN int	lcs_nbsp INIT(= NUL);
 EXTERN int	lcs_tab1 INIT(= NUL);
 EXTERN int	lcs_tab2 INIT(= NUL);
+EXTERN int	lcs_tab3 INIT(= NUL);
 EXTERN int	lcs_trail INIT(= NUL);
 #ifdef FEAT_CONCEAL
 EXTERN int	lcs_conceal INIT(= '-');
diff --git a/src/message.c b/src/message.c
--- a/src/message.c
+++ b/src/message.c
@@ -1658,6 +1658,7 @@ msg_prt_line(s, list)
     int		col = 0;
     int		n_extra = 0;
     int		c_extra = 0;
+    int		c_final = 0;
     char_u	*p_extra = NULL;	    /* init to make SASC shut up */
     int		n;
     int		attr = 0;
@@ -1688,7 +1689,9 @@ msg_prt_line(s, list)
 	if (n_extra > 0)
 	{
 	    --n_extra;
-	    if (c_extra)
+	    if (n_extra == 0 && c_final)
+		c = c_final;
+	    else if (c_extra)
 		c = c_extra;
 	    else
 		c = *p_extra++;
@@ -1724,11 +1727,13 @@ msg_prt_line(s, list)
 		{
 		    c = ' ';
 		    c_extra = ' ';
+		    c_final = NUL;
 		}
 		else
 		{
-		    c = lcs_tab1;
+		    c = (n_extra == 0 && lcs_tab3) ? lcs_tab3 : lcs_tab1;
 		    c_extra = lcs_tab2;
+		    c_final = lcs_tab3;
 		    attr = hl_attr(HLF_8);
 		}
 	    }
@@ -1741,6 +1746,7 @@ msg_prt_line(s, list)
 	    {
 		p_extra = (char_u *)"";
 		c_extra = NUL;
+		c_final = NUL;
 		n_extra = 1;
 		c = lcs_eol;
 		attr = hl_attr(HLF_AT);
@@ -1751,6 +1757,7 @@ msg_prt_line(s, list)
 		n_extra = n - 1;
 		p_extra = transchar_byte(c);
 		c_extra = NUL;
+		c_final = NUL;
 		c = *p_extra++;
 		/* Use special coloring to be able to distinguish <hex> from
 		 * the same in plain text. */
diff --git a/src/option.c b/src/option.c
--- a/src/option.c
+++ b/src/option.c
@@ -7217,7 +7217,7 @@ set_chars_option(varp)
 {
     int		round, i, len, entries;
     char_u	*p, *s;
-    int		c1, c2 = 0;
+    int		c1 = 0, c2 = 0, c3 = 0;
     struct charstab
     {
 	int	*cp;
@@ -7274,8 +7274,12 @@ set_chars_option(varp)
 	    for (i = 0; i < entries; ++i)
 		if (tab[i].cp != NULL)
 		    *(tab[i].cp) = (varp == &p_lcs ? NUL : ' ');
+
 	    if (varp == &p_lcs)
+	    {
 		lcs_tab1 = NUL;
+		lcs_tab3 = NUL;
+	    }
 #if defined(FEAT_WINDOWS) || defined(FEAT_FOLDING)
 	    else
 		fill_diff = '-';
@@ -7291,6 +7295,7 @@ set_chars_option(varp)
 			&& p[len] == ':'
 			&& p[len + 1] != NUL)
 		{
+		    c1 = c2 = c3 = 0;
 		    s = p + len + 1;
 #ifdef FEAT_MBYTE
 		    c1 = mb_ptr2char_adv(&s);
@@ -7310,7 +7315,18 @@ set_chars_option(varp)
 #else
 			c2 = *s++;
 #endif
+			if (!(*s == ',' || *s == NUL))
+			{
+#ifdef FEAT_MBYTE
+			    c3 = mb_ptr2char_adv(&s);
+			    if (mb_char2cells(c3) > 1)
+				continue;
+#else
+			    c3 = *s++;
+#endif
+			}
 		    }
+
 		    if (*s == ',' || *s == NUL)
 		    {
 			if (round)
@@ -7319,6 +7335,7 @@ set_chars_option(varp)
 			    {
 				lcs_tab1 = c1;
 				lcs_tab2 = c2;
+				lcs_tab3 = c3;
 			    }
 			    else if (tab[i].cp != NULL)
 				*(tab[i].cp) = c1;
diff --git a/src/screen.c b/src/screen.c
--- a/src/screen.c
+++ b/src/screen.c
@@ -2841,6 +2841,7 @@ win_line(wp, lnum, startrow, endrow, noc
     int		n_extra = 0;		/* number of extra chars */
     char_u	*p_extra = NULL;	/* string of extra chars, plus NUL */
     int		c_extra = NUL;		/* extra chars, all the same */
+    int		c_final = NUL;		/* final char, mandatory if set */
     int		extra_attr = 0;		/* attributes when n_extra != 0 */
     static char_u *at_end_str = (char_u *)""; /* used for p_extra when
 					   displaying lcs_eol at end-of-line */
@@ -2851,6 +2852,7 @@ win_line(wp, lnum, startrow, endrow, noc
     int		saved_n_extra = 0;
     char_u	*saved_p_extra = NULL;
     int		saved_c_extra = 0;
+    int		saved_c_final = 0;
     int		saved_char_attr = 0;
 
     int		n_attr = 0;		/* chars with special attr */
@@ -3525,6 +3527,7 @@ win_line(wp, lnum, startrow, endrow, noc
 		    /* Draw the cmdline character. */
 		    n_extra = 1;
 		    c_extra = cmdwin_type;
+		    c_final = NUL;
 		    char_attr = hl_attr(HLF_AT);
 		}
 	    }
@@ -3542,6 +3545,7 @@ win_line(wp, lnum, startrow, endrow, noc
 		    p_extra = extra;
 		    p_extra[n_extra] = NUL;
 		    c_extra = NUL;
+		    c_final = NUL;
 		    char_attr = hl_attr(HLF_FC);
 		}
 	    }
@@ -3562,6 +3566,7 @@ win_line(wp, lnum, startrow, endrow, noc
 
 		    /* Draw two cells with the sign value or blank. */
 		    c_extra = ' ';
+		    c_final = NUL;
 		    char_attr = hl_attr(HLF_SC);
 		    n_extra = 2;
 
@@ -3580,9 +3585,13 @@ win_line(wp, lnum, startrow, endrow, noc
 			{
 			    /* Use the image in this position. */
 			    c_extra = SIGN_BYTE;
+			    c_final = NUL;
 #  ifdef FEAT_NETBEANS_INTG
 			    if (buf_signcount(wp->w_buffer, lnum) > 1)
+			    {
 				c_extra = MULTISIGN_BYTE;
+				c_final = NUL;
+			    }
 #  endif
 			    char_attr = icon_sign;
 			}
@@ -3594,6 +3603,7 @@ win_line(wp, lnum, startrow, endrow, noc
 			    if (p_extra != NULL)
 			    {
 				c_extra = NUL;
+				c_final = NUL;
 				n_extra = (int)STRLEN(p_extra);
 			    }
 			    char_attr = sign_get_attr(text_sign, FALSE);
@@ -3651,9 +3661,13 @@ win_line(wp, lnum, startrow, endrow, noc
 #endif
 			p_extra = extra;
 			c_extra = NUL;
+			c_final = NUL;
 		    }
 		    else
+		    {
 			c_extra = ' ';
+			c_final = NUL;
+		    }
 		    n_extra = number_width(wp) + 1;
 		    char_attr = hl_attr(HLF_N);
 #ifdef FEAT_SYN_HL
@@ -3677,9 +3691,15 @@ win_line(wp, lnum, startrow, endrow, noc
 		{
 		    /* Draw "deleted" diff line(s). */
 		    if (char2cells(fill_diff) > 1)
+		    {
 			c_extra = '-';
+			c_final = NUL;
+		    }
 		    else
+		    {
 			c_extra = fill_diff;
+			c_final = NUL;
+		    }
 #  ifdef FEAT_RIGHTLEFT
 		    if (wp->w_p_rl)
 			n_extra = col + 1;
@@ -3695,6 +3715,7 @@ win_line(wp, lnum, startrow, endrow, noc
 		    /* Draw 'showbreak' at the start of each broken line. */
 		    p_extra = p_sbr;
 		    c_extra = NUL;
+		    c_final = NUL;
 		    n_extra = (int)STRLEN(p_sbr);
 		    char_attr = hl_attr(HLF_AT);
 		    need_showbreak = FALSE;
@@ -3720,6 +3741,7 @@ win_line(wp, lnum, startrow, endrow, noc
 		    /* Continue item from end of wrapped line. */
 		    n_extra = saved_n_extra;
 		    c_extra = saved_c_extra;
+		    c_final = saved_c_final;
 		    p_extra = saved_p_extra;
 		    char_attr = saved_char_attr;
 		}
@@ -3914,15 +3936,16 @@ win_line(wp, lnum, startrow, endrow, noc
 	 * The "p_extra" points to the extra stuff that is inserted to
 	 * represent special characters (non-printable stuff) and other
 	 * things.  When all characters are the same, c_extra is used.
+	 * If c_final is set, it will compulsorily be used at the end.
 	 * "p_extra" must end in a NUL to avoid mb_ptr2len() reads past
 	 * "p_extra[n_extra]".
 	 * For the '$' of the 'list' option, n_extra == 1, p_extra == "".
 	 */
 	if (n_extra > 0)
 	{
-	    if (c_extra != NUL)
-	    {
-		c = c_extra;
+	    if (c_extra != NUL || (n_extra == 1 && c_final != NUL))
+	    {
+		c = (n_extra == 1 && c_final != NUL) ? c_final : c_extra;
 #ifdef FEAT_MBYTE
 		mb_c = c;	/* doesn't handle non-utf-8 multi-byte! */
 		if (enc_utf8 && (*mb_char2len)(c) > 1)
@@ -4073,6 +4096,7 @@ win_line(wp, lnum, startrow, endrow, noc
 			mb_utf8 = (c >= 0x80);
 			n_extra = (int)STRLEN(p_extra);
 			c_extra = NUL;
+			c_final = NUL;
 			if (area_attr == 0 && search_attr == 0)
 			{
 			    n_attr = n_extra + 1;
@@ -4141,6 +4165,7 @@ win_line(wp, lnum, startrow, endrow, noc
 			    p_extra = extra;
 			    n_extra = (int)STRLEN(extra) - 1;
 			    c_extra = NUL;
+			    c_final = NUL;
 			    c = *p_extra++;
 			    if (area_attr == 0 && search_attr == 0)
 			    {
@@ -4181,6 +4206,7 @@ win_line(wp, lnum, startrow, endrow, noc
 		{
 		    n_extra = 1;
 		    c_extra = MB_FILLER_CHAR;
+		    c_final = NUL;
 		    c = ' ';
 		    if (area_attr == 0 && search_attr == 0)
 		    {
@@ -4381,6 +4407,7 @@ win_line(wp, lnum, startrow, endrow, noc
 # endif
 				1), (colnr_T)vcol, NULL) - 1;
 		    c_extra = ' ';
+		    c_final = NUL;
 		    if (vim_iswhite(c))
 		    {
 #ifdef FEAT_CONCEAL
@@ -4445,8 +4472,9 @@ win_line(wp, lnum, startrow, endrow, noc
 #endif
 		    if (wp->w_p_list)
 		    {
-			c = lcs_tab1;
+			c = (n_extra == 0 && lcs_tab3) ? lcs_tab3 : lcs_tab1;
 			c_extra = lcs_tab2;
+			c_final = lcs_tab3;
 			n_attr = n_extra + 1;
 			extra_attr = hl_attr(HLF_8);
 			saved_attr2 = char_attr; /* save current attr */
@@ -4462,6 +4490,7 @@ win_line(wp, lnum, startrow, endrow, noc
 		    }
 		    else
 		    {
+			c_final = NUL;
 			c_extra = ' ';
 			c = ' ';
 		    }
@@ -4511,6 +4540,7 @@ win_line(wp, lnum, startrow, endrow, noc
 			    p_extra = at_end_str;
 			    n_extra = 1;
 			    c_extra = NUL;
+			    c_final = NUL;
 			}
 		    }
 		    if (wp->w_p_list)
@@ -4545,6 +4575,7 @@ win_line(wp, lnum, startrow, endrow, noc
 #endif
 		    n_extra = byte2cells(c) - 1;
 		    c_extra = NUL;
+		    c_final = NUL;
 		    c = *p_extra++;
 		    if (!attr_pri)
 		    {
@@ -4759,6 +4790,7 @@ win_line(wp, lnum, startrow, endrow, noc
 		/* Double-width character being overwritten by the "precedes"
 		 * character, need to fill up half the character. */
 		c_extra = MB_FILLER_CHAR;
+		c_final = NUL;
 		n_extra = 1;
 		n_attr = 2;
 		extra_attr = hl_attr(HLF_AT);
@@ -5413,6 +5445,7 @@ win_line(wp, lnum, startrow, endrow, noc
 	    saved_n_extra = n_extra;
 	    saved_p_extra = p_extra;
 	    saved_c_extra = c_extra;
+	    saved_c_final = c_final;
 	    saved_char_attr = char_attr;
 	    n_extra = 0;
 	    lcs_prec_todo = lcs_prec;

