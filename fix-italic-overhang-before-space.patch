# HG changeset patch
# Parent 0d562ac6a11939a577c936226c816a3387acbd03
# Parent  2df22f53f86e5b8331b1ce344356fea81c617538

diff --git a/src/gui.c b/src/gui.c
--- a/src/gui.c
+++ b/src/gui.c
@@ -2758,7 +2758,11 @@ gui_redraw_block(
 	// is.  Needed for when the bold trick is used
 	for (back = 0; back < col1; ++back)
 	    if (ScreenAttrs[off - 1 - back] != ScreenAttrs[off]
-		    || ScreenLines[off - 1 - back] == ' ')
+		    || (ScreenLines[off - 1 - back] == ' '
+			&& (off - 2 - back < 0
+			    || ScreenLines[off - 2 - back] == ' ')
+			&& (off - 3 - back < 0
+			    || ScreenLines[off - 3 - back] == ' ')))
 		break;
 
 	// Break it up in strings of characters with the same attributes.
