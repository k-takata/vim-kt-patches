# HG changeset patch
# Parent  81ccdf6e25eb5260e90ba4ca2ea328832b7f7005

diff --git a/src/GvimExt/gvimext.cpp b/src/GvimExt/gvimext.cpp
--- a/src/GvimExt/gvimext.cpp
+++ b/src/GvimExt/gvimext.cpp
@@ -158,6 +158,7 @@ HBITMAP IconToBitmap(HICON hIcon, HBRUSH
 # define VIMPACKAGE "vim"
 # ifndef GETTEXT_DLL
 #  define GETTEXT_DLL "libintl.dll"
+#  define GETTEXT_DLL_ALT "libintl-8.dll"
 # endif
 
 // Dummy functions
@@ -183,6 +184,7 @@ static char *(*dyn_libintl_bindtextdomai
 dyn_libintl_init(char *dir)
 {
     int		i;
+    int		len;
     static struct
     {
 	char	    *name;
@@ -199,16 +201,20 @@ dyn_libintl_init(char *dir)
     if (hLibintlDLL)
 	return 1;
 
-    // Load gettext library, first try the Vim runtime directory, then search
-    // the path.
-    strcat(dir, GETTEXT_DLL);
+    // Load gettext library from the Vim runtime directory
+    len = strlen(dir);
+#ifdef GETTEXT_DLL_ALT
+    strcat(dir, GETTEXT_DLL_ALT);
     hLibintlDLL = LoadLibrary(dir);
+#endif
     if (!hLibintlDLL)
     {
-	hLibintlDLL = LoadLibrary(GETTEXT_DLL);
-	if (!hLibintlDLL)
-	    return 0;
+	dir[len] = '\0';
+	strcat(dir, GETTEXT_DLL);
+	hLibintlDLL = LoadLibrary(dir);
     }
+    if (!hLibintlDLL)
+	return 0;
 
     // Get the addresses of the functions we need.
     for (i = 0; libintl_entry[i].name != NULL
